# CLAUDE.md

妖怪ウォッチ2の対人戦システムを元にした、ブラウザ向けの 3v3 リアルタイム・ローテーション対戦ゲーム。

## 最初に読むもの
- `docs/PLAN.md` — 企画・決定事項・ロードマップ
- `docs/BATTLE_SPEC.md` — 戦闘仕様（**戦闘ロジックの唯一の正**）
- `docs/UNITS.md` — ユニット数値

## 守ること
- **戦闘コアは決定論**：浮動小数点・`Math.random`・`Date.now` を使わない。倍率は‰（1000 = 1.0倍）の整数、割り算は切り捨て。
- 乱数はシード付きの疑似乱数（BATTLE_SPEC §4.5）だけを使う。
- 戦闘コアは描画・通信に依存しない純粋な TypeScript ライブラリにする。
- 仕様と違う実装をするときは、先に `docs/BATTLE_SPEC.md` を直して理由を書く。
- 1 tick の処理順（BATTLE_SPEC §3）を変えない。変えるときはリプレイの互換が壊れることを明記する。
- ルールの変更には単体テストを付ける。
- **IP**：妖怪ウォッチのキャラ名・デザイン・固有の用語をコード・アセット・UI 文言に使わない。docs の分析セクションの中だけで参照する。
