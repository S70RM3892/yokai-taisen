{
  "name": "yokai-taisen",
  "version": "1.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/check_names.mjs && node tools/build.mjs",
    "test": "node tools/build.mjs --engine-only && node tests/sim.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "peer": "^1.0.2"
  }
}
