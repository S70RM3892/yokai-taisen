{
  "name": "yokai-taisen",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "test": "node tools/build.mjs --engine-only && node tests/sim.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
